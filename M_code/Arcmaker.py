import gurobipy as gp
from gurobipy import GRB
import numpy as np
from tqdm import tqdm

#this file contains the functions to create the arcs based on the model selected

def MakeArcs(model,nodes,nodedist):
    
    return